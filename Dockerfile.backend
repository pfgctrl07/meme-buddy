FROM python:3.11-slim

WORKDIR /app

COPY backend/requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY backend ./backend

WORKDIR /app/backend

ENV FLASK_DEBUG=0

CMD ["sh", "-c", "gunicorn --bind 0.0.0.0:${PORT:-5001} wsgi:app"]
